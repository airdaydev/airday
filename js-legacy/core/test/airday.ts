// Tests the materialisation of Airday objects
// Later, this can be extracted from the core sync package

import { test, expect } from "bun:test";

test("Tasks", async () => {});
test("Calendar events", async () => {});
