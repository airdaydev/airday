{
  "name": "@airday/tracer",
  "version": "0.0.1",
  "description": "Ultra-light tracing. OpenTelemetry compatible, hopefully.",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "bun run bundle.ts",
    "dev": "bun run bundle.ts --watch",
    "clean": "rm -rf dist",
    "tsc": "tsc -p tsconfig.json --declaration --emitDeclarationOnly --outDir dist",
    "prepublishOnly": "bun run clean && bun run build"
  },
  "files": [
    "dist"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.3"
  }
}
