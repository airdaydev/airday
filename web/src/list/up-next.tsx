/**
 * Up next is a special list which takes ReferenceItems only
 * By default we will show the list the item comes from to make this clear to the user
 */

export const Done = () => {
    return (
        <div>
            A list of historical items
        </div>
    )
}
