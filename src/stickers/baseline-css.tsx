import { Modifiers } from './types';

const Triangle = (modifiers: Modifiers) => (
    <div></div>
);
const Square = (modifiers: Modifiers) => (
    <div></div>
);
const Circle = (modifiers: Modifiers) => (
    <div></div>
);

export const BaselineCSSStickers = {
    triangle: Triangle,
    square: Square,
    circle: Circle,
};
